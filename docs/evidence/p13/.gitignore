# Rendered manifests are INPUTS to the receipt, not evidence themselves: they
# are regenerated by scripts/topology_receipt.sh on every run and would go
# stale in the repository while still looking authoritative. The receipt
# records the digests, object counts, and check results that matter.
rendered-*.yaml
