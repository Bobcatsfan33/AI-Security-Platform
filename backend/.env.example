# --- Application ---
ENVIRONMENT=development
LOG_LEVEL=INFO
API_V1_PREFIX=/v1
CORS_ORIGINS=http://localhost:3000

# --- PostgreSQL (operational data) ---
DATABASE_URL=postgresql+asyncpg://platform:platform@localhost:5432/platform

# --- ClickHouse (telemetry — wired in Sprint 1 follow-on) ---
CLICKHOUSE_URL=http://localhost:8123
CLICKHOUSE_DATABASE=telemetry

# --- Redis (cache, pub/sub, session revocation) ---
REDIS_URL=redis://localhost:6379/0

# --- Redpanda (event streaming — wired in Sprint 7) ---
REDPANDA_BROKERS=localhost:9092

# --- JWT signing ---
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING_AT_LEAST_64_CHARS_FOR_DEV_ONLY
JWT_ALGORITHM=HS256
JWT_ACCESS_TTL_SECONDS=900
JWT_REFRESH_TTL_SECONDS=604800

# --- OIDC client secret resolution (dev-mode env-var vault) ---
# In production, replace VaultEnvResolver with a real secret manager (AWS SM, Vault, etc.)
# Format for env-var resolution: client_secret_ref="env:OIDC_SECRET_<idp_id>"
